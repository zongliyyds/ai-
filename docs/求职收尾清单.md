# VaultMind 求职收尾清单（用户侧动作 · 按顺序执行）

> 这些动作需要你的账号与人工操作，AI 无法代办。全部就绪 = 项目彻底完成，可交付《面试答辩手册 PDF》配套使用。

## 1. GitHub 发布 ✅ 已完成（2026-09-17，AI 代办）

1. ~~浏览器打开 github.com 登录 → New repository~~ 已推到既有私有仓库：`https://github.com/zongliyyds/ai-`（origin/main，97 文件，本地与远端 HEAD 一致）。
2. 后续推送只需（凭据已存本机 Windows 凭据管理器，无需再登录）：
   ```powershell
   cd /d D:\RAG
   git push
   ```
3. **仓库当前保持 Private**（用户 2026-09-17 明确不公开）；`gh-proxy` 镜像这次**未用到**——本机 git 走 HTTPS 直连可达（Python/urllib 与 git 均实测 200），失败时才需要镜像。
4. 可选（未做）：把 `dist/vaultmind-source-*.zip` 作为 Release 附件（无 Vault 内容的干净源码包）。

## 1b. 若要公开（用户决定时再执行）

- Settings → General → Danger Zone → Change visibility → Public；公开前建议确认 `docs/立项书-详细版.md` 是否随仓库一起可见（私有下无风险）。

## 2. 录屏（~3 分钟，建议 Xbox Game Bar：Win+G）

脚本顺序（对应立项书演示素材）：

1. （0:00）打开 `run_api.bat` → 浏览器 `http://127.0.0.1:8001`
2. （0:20）问答页提问「CET-4 项目用了什么去重方案？」→ 展示答案 + 点击 [S#] 引用跳转 Obsidian 原笔记
3. （0:50）再问一个超纲问题（如"今天比特币价格"）→ 展示拒答
4. （1:10）切「分析看板」→ 库规模 / 基线指标达标条 / 坏例账本
5. （1:40）终端跑 `python -m vaultmind.eval` → 指标表复现
6. （2:10）展示 `reports/ablation.md` 六组消融表 + 负结果结论
7. （2:40）收尾一句话：项目定位 + 三条简历 bullet

> 录屏前自检：`ollama serve` 运行中（curl http://127.0.0.1:11434/api/tags）；跑一遍 `scripts/env_check.py` 全 PASS。
>
> **服务启动（2026-09-17 更新）**：双击 `run_api.bat` 即可（已修：端口检测改用 TCP 探测，不再因本机 `netstat` 失效而假判"已在运行"并秒关窗）。窗口**保持打开=服务运行中**，关闭窗口=停止服务；启动日志在 `data\api_server.log`。若浏览器报「拒绝连接」= 服务没在跑，重新双击即可（服务不是开机自启）。
> **只想检索、不起服务**：双击或命令行 `search.bat "你的问题"`（直接读索引库，秒回）。

## 3. 面试前自检

- [ ] `scripts/env_check.py` 13 项全 PASS
- [ ] `pytest tests -q` 全绿（56+ 条）
- [ ] `docs/面试答辩手册.pdf` 已生成且能打开（中文正常）
- [ ] 通读 `docs/Q&A预案.md`（含数字速查表）
- [ ] `docs/简历bullet.md` 两条方向各选 3~4 条贴入简历

## 4. 红线自查（打包探针同款口径）

- [ ] GitHub 仓库不含 `data/`（索引）与任何 Vault 内容（打包探针已过，推 git 时注意 `.gitignore` 生效）
- [ ] 简历数字全部能在 `reports/` 找到出处
